# Fake values used only when running tests (client.ts throws at import time without them).
# Not secrets — safe to commit. Real .env stays untracked and is used for local dev.
VITE_API_URL=http://localhost/api
VITE_API_KEY=test-key
VITE_APP_NAME=CPA Snake Landing
